{
  "name": "BinaryThreshold",
  "label": "Binary Threshold",
  "description": "Threshold image. Voxels with values between the lower and upper thresholds are considered object, others background. The sliders span the data's actual range and follow a Threshold visualization on the same data while the dialog is open, so the segmentation matches what you see.",
  "externalCompatible": true,
  "widget": "ThresholdRangeWidget",
  "children": [
    {
      "name": "thresholded_segmentation",
      "label": "Thresholded Segmentation",
      "type": "label_map"
    }
  ],
  "parameters": [
    {
      "name": "lower_threshold",
      "label": "Lower Threshold",
      "type": "double",
      "default": 40.0,
      "bindToSink": {
        "type": "ThresholdSink",
        "property": "lowerThreshold"
      }
    },
    {
      "name": "upper_threshold",
      "label": "Upper Threshold",
      "type": "double",
      "default": 255.0,
      "bindToSink": {
        "type": "ThresholdSink",
        "property": "upperThreshold"
      }
    }
  ],
  "inputType": "ImageData",
  "outputType": "LabelMap"
}
